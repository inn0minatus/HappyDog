{
  "name": "happy-dog-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Happy Dog Runner — a branded, mobile-first 2D browser runner (Phaser 3 + TypeScript + Vite).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "gen:placeholders": "tsx scripts/gen-placeholders.mts",
    "deploy": "npm run build && gh-pages -d dist -t"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
